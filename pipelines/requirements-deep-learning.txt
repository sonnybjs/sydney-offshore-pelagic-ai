# Optional deep-learning dependencies for independent SDM experiments.
# Keep this separate from pipelines/requirements.txt so the original
# scikit-learn pipeline remains lightweight and easy to run.
torch>=2.2
scikit-learn>=1.4,<2
pandas>=2.2,<3
numpy>=1.26,<3
pyarrow>=15,<23
matplotlib>=3.8,<4
